# Base de datos
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=
DB_DATABASE=DBAsocomunales

# JWT (reemplazar con un secreto seguro antes de producción)
JWT_SECRET=

# RabbitMQ
RABBITMQ_URL=amqp://localhost:5672

# Aplicación
PORT=3001
CORS_ORIGIN=http://localhost:5173

# Cookie (nombre de la cookie HTTP Only que envía el frontend)
COOKIE_NAME=ejemplo_token
